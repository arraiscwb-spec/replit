## Packages
framer-motion | Smooth scroll animations and entry effects for a premium feel
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
The sales page relies on smooth scrolling and high-impact visual sections.
Images are sourced from the provided URLs in the prompt.
The API contract for leads is defined but likely not the primary focus of a sales page (which links to an external checkout), but I will include a lead capture form if appropriate or just keep the hooks ready.
Theme: Vibrant Purple/Indigo.
